using System;

namespace fido2NetLib
{
    internal static class Parser
    {
        public static object ParseClientResponse(object Request)
        {
            // todo: not sure if we should use a parsing class (instead of doing it in the response/result classes). This might be removed from the API.
            throw new NotImplementedException();
        }
    }
}
